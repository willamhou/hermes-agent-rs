name: Temporary Guanlan Horizon Pages Probe

on:
  pull_request:
    paths:
      - tmp_tools/guanlan_pages_probe.py
      - .github/workflows/tmp-guanlan-pages-probe.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  pages-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests beautifulsoup4
      - run: python tmp_tools/guanlan_pages_probe.py
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: guanlan-horizon-pages-probe
          path: guanlan_pages_probe
          if-no-files-found: warn
          retention-days: 2
