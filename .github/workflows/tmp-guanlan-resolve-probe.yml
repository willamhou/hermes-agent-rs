name: Temporary Guanlan Horizon Resolve Probe

on:
  pull_request:
    paths:
      - tmp_tools/guanlan_resolve_probe.py
      - .github/workflows/tmp-guanlan-resolve-probe.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  resolve-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests beautifulsoup4
      - run: python tmp_tools/guanlan_resolve_probe.py
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: guanlan-horizon-resolve-probe
          path: guanlan_resolve_probe
          if-no-files-found: warn
          retention-days: 2
